version: '3.8'

services:
  frontend:
    build:
      context: ./NoBullshitReviews.Frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - app-network

  backend:
    build:
      context: ./NoBullshitReviews.Api
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
